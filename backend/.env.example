DJANGO_SECRET_KEY=troque-esta-chave-por-uma-segura-em-producao
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
# Em produção (ex: Neon/PostgreSQL):
# DATABASE_URL=postgres://user:password@host:5432/dbname
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000
SITE_URL=https://tiagobanze.dev

# Em produção: guarda avatar, CV e certificados no Cloudinary (grátis),
# porque o Vercel apaga ficheiros enviados a cada novo deploy.
# Formato: cloudinary://<api_key>:<api_secret>@<cloud_name>
CLOUDINARY_URL=
